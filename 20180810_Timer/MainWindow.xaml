<Window x:Class="_20180810_Timer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20180810_Timer"
        mc:Ignorable="d"
        Title="MainWindow" Height="130" Width="600" Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="131*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" FontSize="44" Text="{Binding Text[0], FallbackValue=--:--}" />
        <TextBlock Grid.Row="1" Grid.Column="0" FontSize="22" Text="{Binding Text[1], FallbackValue=--:--}" />

    </Grid>
</Window>
